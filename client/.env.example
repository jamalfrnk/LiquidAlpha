# Base URL of the LiquidAlpha API server (see server/.env.example).
VITE_API_URL=http://localhost:3001

# WebSocket server URL (separate port from the HTTP API -- see server.ts).
VITE_WS_URL=ws://localhost:8080
