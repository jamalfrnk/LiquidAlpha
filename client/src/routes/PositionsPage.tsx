import { Layers } from 'lucide-react';
import { PlaceholderPage } from '../components/PlaceholderPage';

export function PositionsPage() {
  return (
    <PlaceholderPage
      icon={Layers}
      title="Positions is coming next"
      description="Order submission, cancellation, and position close already work end to end on the backend (paper trading, fully risk-gated). The order ticket and positions table are the next build."
    />
  );
}
