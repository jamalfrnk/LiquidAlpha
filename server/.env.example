# Configuration for the LiquidAlpha backend

# PostgreSQL connection string.  Replace with your own credentials and
# database name.  See https://www.postgresql.org/docs/current/libpq-connect.html
# for connection URI format.
DATABASE_URL=postgresql://user:password@localhost:5432/liquidalpha

# Secret used to sign JSON Web Tokens for authentication.  Set this to a
# strong random value in production.  Not used directly in this example
# server but reserved for future auth features.
JWT_SECRET=replace-with-your-secret

# Port number for the HTTP server.  Defaults to 3001 if not set.
PORT=3001

# Port number for the WebSocket server.  Defaults to 8080 if not set.
WS_PORT=8080

# Optional API keys

# CoinGecko API key for higher rate limits on price data.  Leave unset to
# use the free tier.  See https://www.coingecko.com/en/api for details.
COINGECKO_API_KEY=

# Hyperliquid API URL and key.  The URL defaults to the public RPC if
# unset.  Provide a key here if you have access to premium endpoints.
HYPERLIQUID_API_URL=https://api.hyperliquid.xyz
HYPERLIQUID_API_KEY=
